package ui;

public interface CheckWinCon {
	/**
	 * 
	 * @param game The Game to check the win Condition of
	 * @return true if winCondition met, false otherwise
	 */
	public boolean checkWinCondition(Game game);
}
